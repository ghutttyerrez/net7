// Central configuration for the official WhatsApp number used across the site.
// Keep only digits including country and area code (DDI + DDD if desired per strategy).
export const WHATSAPP_NUMBER = '5567993259746'
export const WHATSAPP_BASE_URL = `https://wa.me/${WHATSAPP_NUMBER}`
